FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY mcp-servers/order_management_mcp.py .

EXPOSE 8000

CMD ["python", "-m", "uvicorn", "order_management_mcp:app", "--host", "0.0.0.0", "--port", "8000"]
