FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY agents/order_manager_agent.py .

EXPOSE 8080

CMD ["python", "-m", "uvicorn", "order_manager_agent:app", "--host", "0.0.0.0", "--port", "8080"]
