apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-service-agent
  labels:
    app: customer-service-agent
spec:
  replicas: 2
  selector:
    matchLabels:
      app: customer-service-agent
  template:
    metadata:
      labels:
        app: customer-service-agent
    spec:
      containers:
      - name: customer-service-agent
        image: ${REGION}-docker.pkg.dev/${PROJECT_ID}/boutique-ai-repo/customer-service-agent:latest
        ports:
        - containerPort: 8080
        env:
        - name: PROJECT_ID
          value: "${PROJECT_ID}"
        - name: REGION
          value: "${REGION}"
        - name: A2A_NETWORK_ENDPOINT
          value: "a2a-network:8080"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: customer-service-agent
spec:
  selector:
    app: customer-service-agent
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
